# =============================================================================
# versions.yaml — the single source of truth for every byte packed-metanorma
# assembles (spec 00 §10: one owner; everything else flows from here).
#
# packed-metanorma compiles nothing: every input is a published, sha256-pinned
# release artifact. Bumping ANY input is a one-line edit of this file
# (the release trigger flow writes it; the workflow reads it; nothing
# else in the repo hardcodes a version or a digest).
#
# Grammar (schema_version 1):
#   tebako.version / .release   — the tamatebako/tebako toolchain release
#   tebako.sha256.<tool>.<tool-platform>   — digest of the release asset
#       <tool>-<version>-<tool-platform>[.exe]; tools: tebako-pkg,
#       tebako-bootstrap, tfs (the press legs), tebako + tebako-shim (the
#       installer legs stage the four PATH tools — spec 16 §7; their
#       macos-* pins live here too); tool platforms: macos-arm64,
#       macos-x86_64, linux-gnu-x86_64, windows-ucrt64
#   runtime                       — the tebako-runtime-ruby line the
#       package resolves at first run (lean: downloaded once per machine,
#       sha256-verified against the release's own manifest/SHA256SUMS.txt)
#   runtime.sha256.<asset-kind>.<tool-platform> — the exe / env-image /
#       windows dll digests (the resolution anchor; also the future fat
#       composition's slot pins)
#   payloads[]                    — the .tfs slices, keyed by FEEDSTOCK
#       triplet (aarch64-macos, x86_64-linux-gnu, x86_64-windows-ucrt)
#       with the release tag + asset file name + sha256 mirrored from the
#       feedstock's tpkg-registry.yaml (the registries are the upstream
#       SSOT for these digests — this file mirrors them by value).
# =============================================================================

schema_version: 1

tebako:
  version: 2.2.0
  release: v2.2.0
  sha256:
    tebako-pkg:
      macos-arm64: 5d93c27cc7ee51b87a023327cf78bbddb0a8ddf7dd831a49e45fcc2e301321c8
      macos-x86_64: 65b852b6fe72618344a605852068f841a6fb367eed5aa1eee98b24087cf3b11f
      linux-gnu-x86_64: 80fe52205365325e76f07b27ae38522da33994fd91162369ba7b02f63b54f066
      windows-ucrt64: 244ec46e68722909c945c1f9f85cda966b0c58fc8fde629d5c3870d84a9ed882
    tebako-bootstrap:
      macos-arm64: e6ab3bac0c90e713a97753899fe26598b8d5378b1fc10d838c1a795ac198c106
      linux-gnu-x86_64: 1fcfe66c834168c15cf1e07b6c539ec79ee332f828e40b8d3f246d2decb38923
      windows-ucrt64: d49887fff9dcf3689ea837e3471ccd3e9b43f1f72820278121412109db5ec7c2
    tfs:
      macos-arm64: 7d1aeb18258beb8c19ead69fa5fb72e65011d0c3277c2ce4c3e521282d15c91b
      macos-x86_64: d7399848287b0feef163dbb3c3ce7f1819fb0cdb888d5a73573f0c014d519915
      linux-gnu-x86_64: 74e481a76e0b70f80eaabbda77a8b0e5a10fd8300bcf382ee7e03b07be50bef8
      windows-ucrt64: 8e015a285899ac42164f46341ab4e2f5039eb782b46cf49cb6c9cade4cd68521
    # The installer legs (spec 16 §7) stage the FOUR PATH tools — tebako,
    # tebako-shim, tfs, tebako-pkg — so the CLI + dispatcher pins join the
    # press set here. macOS only: the live installer legs are the two pkg
    # arch legs; the windows-ucrt64 pins land with the parked MSI leg's
    # unpark (pins.rb fails CLOSED on a missing pin, named).
    tebako:
      macos-arm64: a588282aa712b09c3d4e16c83eddd1878be735bae0faa7dbe3fb0da4b074d665
      macos-x86_64: f42379a427ca11f50f9f8cddeaca7a01b3adb40e55f306d5eb2066614e4ae3d0
    tebako-shim:
      macos-arm64: 75c96eddb9a8a194dbb50be55c680d1c769884bcc2d2f5d835145ffd1e9ebccd
      macos-x86_64: b85cd0242cc173cba7b96ea9a628ba077ce36d3371141ce928ea06c8850b6871

runtime:
  # The runtime reference every package entry carries:
  #   ruby@3.3.12;tebako=0.16.22;image
  # ruby stays on the 3.3 line ON PURPOSE: the benchmark comparison vs the
  # old (v1-tebako) packed-metanorma executables holds the interpreter line fixed
  # (owner decision 2026-08-26 — do NOT bump to 4.0.6 here).
  # 0.16.22 is REQUIRED by the spec-32 edge, not a preference: it is the
  # factory re-cut on the tebako v2.2.0 link unit (the schema_minor-5
  # driver parse) — on 0.16.21 the runtime exe's embedded driver refuses
  # the edge-carrying metanorma manifest at boot by name ("unknown
  # variant `executable`").
  release: v0.16.22 # tamatebako/tebako-runtime-ruby tag
  tebako: 0.16.22 # the runtime line (tebako=<abi> in the ref)
  ruby: 3.3.12 # the interpreter version
  sha256:
    exe:
      macos-arm64: 2a4bc5a9ad8860fa884586e9bcac87b14d7f5f41451a0e035ab8f0f77800f954
      linux-gnu-x86_64: 5a2b6c8303999af0b5c73d6125074de192af82cf86b58ca22567da7bcb010a72
      windows-ucrt64: ac6b9d8c8e56c61ebb00925c76f0f5b4a7fa910f3dbce258cc9971a0e8446ed4
    image:
      macos-arm64: 1519215e7d998ef02300580d56f06fd72a9ee6fb61da3a1a1ff8d98f3c1272d4
      linux-gnu-x86_64: dae5c5d66887b26281ca6f86f33d3f94fef4c0eadcac0625442723cbcfd6565e
      windows-ucrt64: c739320ce6383c94aa7ebd8a9cac910a94d02aa8765c8a2a0f20d73af295ebde
    dll: # windows-only facet (install_as: x64-ucrt-ruby330.dll)
      windows-ucrt64: 685f1b86cf264c17a29b98b16dbfc214a38edb277cfe606452d89f8b8b203ba2

package:
  name: metanorma
  # The user-facing version = the metanorma payload version (the release
  # tag is v<this>). The composition's truth lives in the L2 package
  # manifest + the release's SHA256SUMS, not in an invented composite.
  version: 1.16.9

# The installer containers (spec 16 §7): the product also ships as a
# system pkg (macOS; the MSI parks with the windows press leg — see
# build.yml), built by tamatebako/tebako's ci scripts + templates, fetched
# at installer_ref (the SSOT — never forked). Bound as a WEB BOOTSTRAPPER:
# the installer stages the four PATH tools (the tebako/tebako-shim/tfs/
# tebako-pkg pins above), runs <root>/bootstrap-seed.* at install time
# (best-effort — an offline machine re-runs it later) to install the
# metanorma payload from the feedstock registry, WARMS its runtime into
# the root-owned machine home (a user's dispatch is read-only after the
# warm), and authors the <root>/home/shims store-grammar marker
# (spec 05 §3.1) so the bundle's tools resolve the bundle home
# zero-config.
installers:
  product_name: metanorma
  manufacturer: Metanorma
  org_id: org.metanorma
  install_root: /opt/metanorma # the pkg's root; the MSI roots at Program Files\metanorma
  # The seed installs the same-named payload from the FEEDSTOCK's registry
  # (payloads flow from tebako-packages/metanorma's published releases —
  # never from this repo's).
  registry: tfs:github:tebako-packages/metanorma
  # The installer machinery pin: a tamatebako/tebako main sha (the
  # spec 16 §7 web-bootstrapper hook). 212b2cb carries the client-product
  # rehearsal fix (#606 — the rehearsal asserts the toolset binary + the
  # warmed shims, not bin/<product>); earlier shas fail every client
  # product's rehearsal.
  installer_ref: 212b2cb778debcb42da88f3ca6a74273c6656ca2
  # The MSI UpgradeCode — LOCKED FOREVER (MSI upgrades pair on it). Minted
  # 2026-09-15 for this product line; never reuse tebako's own GUID. Only
  # the (parked) MSI leg consumes it.
  msi_upgrade_code: 57CE2E16-2EA0-4B1C-AD8F-7E66F82D0EAA

payloads:
  - name: metanorma
    version: 1.16.9 # the slice version (lock.slices[].version)
    release: 1.16.9-9 # tebako-packages/metanorma tag (see Deviations: not "1.16.9")
    assets:
      aarch64-macos:
        file: metanorma-1.16.9-macos-arm64.tfs
        sha256: a36e9733944fe5d82d1542c30d587ccaeb2129b927c9bdeee6d6549068701787
      x86_64-linux-gnu:
        file: metanorma-1.16.9-linux-gnu-x86_64.tfs
        sha256: a091af9e4e4b21537f25dff9c3dc4210f06733b568b7f917f741734f3abc9622
      x86_64-windows-ucrt:
        file: metanorma-1.16.9-windows-ucrt64.tfs
        sha256: '23abaf44a071251579e4b0488636397e77366425e805220393775639723bf7eb'
  - name: openjdk
    # openjdk is now a RUNTIME-kind payload: the v2.1.5 feedstock release
    # renamed the assets to the runtime grammar (tebako-runtime-<tools>-<ver>-*).
    # spec 30: it is NEVER co-mounted — the package carries the exe+image
    # pair as lock.spawned[]-claimed slots; the bootstrap stages them into
    # the machine runtime cache at dispatch and exports TEBAKO_SPAWN_LOCK.
    # v2.4.0 (REQUIRED 2026-09-06): the pair's launcher must read spec 32's
    # kind: executable requires edge — 1.16.9-9 declares it (xml2rfc spawn)
    # and the 2.1.5/2.3.0 launchers refuse it by the evolution law ("Jing
    # failed … unknown variant `executable`"). Without this bump the carried
    # java runtime breaks every jing/mn2pdf-spawning compile.
    release: v2.4.0 # tebako-packages/openjdk tag
    lang_version: 21.0.12 # the engine version (lock.spawned[].version)
    tebako_version: 2.4.0 # the runtime's own tebako line (lock.spawned[].tebako)
    assets:
      aarch64-macos:
        file: tebako-runtime-2.4.0-21.0.12-macos-arm64.tfs
        sha256: af3a0cadbb7d260ec4b369dc866f4e40923fd9e8266040b89d72ca2c0d78e9d7
        exe_file: tebako-runtime-2.4.0-21.0.12-macos-arm64
        exe_sha256: b27b44f560af3935458066e6d8815a8c985025ddf9657483303b45ad98e1db4c
      x86_64-linux-gnu:
        file: tebako-runtime-2.4.0-21.0.12-linux-gnu-x86_64.tfs
        sha256: 66259d5dcf50fda827664ef798d7ffa20376f4414ab4af75c2de6bf82b5b1af3
        exe_file: tebako-runtime-2.4.0-21.0.12-linux-gnu-x86_64
        exe_sha256: 910fc6d538e624150c9967564c67aefa277686ec00a77d898de8d78753ff12a7
      x86_64-windows-ucrt:
        file: tebako-runtime-2.4.0-21.0.12-windows-ucrt64.tfs
        sha256: f756e81838eb45e369e2185d13ba31dbc5f8448aec027cdc8f3b4d7a6757b6d9
        exe_file: tebako-runtime-2.4.0-21.0.12-windows-ucrt64.exe
        exe_sha256: a97fc14d15f354e561c8c8c2bcfd007469fff343a58d0883de8181b85eee66f9
  - name: inkscape
    version: 1.4.3 # the slice version (lock.slices[].version)
    release: 1.4.3-2 # tebako-packages/inkscape tag
    assets:
      aarch64-macos:
        file: inkscape-1.4.3-aarch64-macos.tfs
        sha256: 68a6b3717c1a01249e0d2ff2e9f9c41f8c4441faaeeb65657e18b7c2e7ea2a7a
      x86_64-linux-gnu:
        file: inkscape-1.4.3-x86_64-linux-gnu.tfs
        sha256: 56aa809b13c0b5875e98fcfea0facbb172d8aa51a5860e1ccdafeebd3183ce45
      x86_64-windows-ucrt:
        file: inkscape-1.4.3-x86_64-windows-ucrt.tfs
        sha256: 1df2edb60ecaaef46c0f077b9421b597ef975ba31b076ef8a2ae3d0a971f4292
  - name: xml2rfc
    # spec 32's first spawned-PAYLOAD edge: the ietf chain's xml2rfc console
    # script, dispatched as a child process through its own spec-17
    # composition on the python runtime — NEVER co-mounted (the payload
    # image + the nested python pair ride lock.spawned[]-claimed slots,
    # exactly like openjdk above).
    #
    # RELEASE CHANNELS (re-flowed 2026-09-06 at the publish go-ahead): the
    # payload digests mirror tebako-packages/xml2rfc's published registry
    # (tag 3.34.0 — the first payload release; the POSIX six, windows
    # pending the feedstock's windows leg); the python pair mirrors
    # tamatebako/tebako-runtime-python's PUBLISHED v0.1.0 SHA256SUMS (the
    # factory's first line, python 3.13.15).
    version: 3.34.0 # the slice version (lock.spawned[] payload row's version)
    release: 3.34.0 # tebako-packages/xml2rfc tag
    lang_version: 3.13.15 # the nested runtime row's language version
    tebako_version: 0.1.0 # the python runtime's own tebako line
    python_release: v0.1.0 # tamatebako/tebako-runtime-python tag
    assets:
      aarch64-macos:
        file: xml2rfc-3.34.0-macos-arm64.tfs
        sha256: dd4e43954ed00af526ca52ba8e8470b7f83505817acad087b919d4a3de2fd0a7
        py_exe_file: tebako-runtime-0.1.0-3.13.15-macos-arm64
        py_exe_sha256: 4a45e648e3b0e5d038d220ad62e572fd9a952517dbd59970c76929c7435eb955
        py_image_file: tebako-runtime-0.1.0-3.13.15-macos-arm64.tfs
        py_image_sha256: c0fbd0bb7787d858003825b5d7bb33f3a88bd01ff1ab5798daaaa2b631b8df90
      x86_64-linux-gnu:
        file: xml2rfc-3.34.0-linux-gnu-x86_64.tfs
        sha256: 5f55766ad63f78edd67d771dc1530f40d702236b1894294e85217d3b25611579
        py_exe_file: tebako-runtime-0.1.0-3.13.15-linux-gnu-x86_64
        py_exe_sha256: 67c828a3b306125989ae777630e445bde5c775c4e0e4dd1394874cd0560b6b3e
        py_image_file: tebako-runtime-0.1.0-3.13.15-linux-gnu-x86_64.tfs
        py_image_sha256: 9eb034bd28d8ae97cb9d618d2a68c49c47c549172a72c0927905d85f8ba25351
      # x86_64-windows-ucrt: deliberately ABSENT — no windows xml2rfc
      # payload (the feedstock's CI is POSIX-only today) and no windows
      # python runtime (the factory run built six POSIX legs) exist.
      # pins.rb's missing-asset named error fails the windows press leg
      # CLOSED; the leg is gated on the xml2rfc feedstock's windows build,
      # a follow-up. Never invent a placeholder here.
